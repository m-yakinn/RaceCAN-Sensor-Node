# Testing Checklist

This checklist verifies that RaceCAN Digital Kit V1 works correctly.

## Simulator Tests

### Normal Mode

Command:

```bash
cd software/simulator
python simulator.py --mode normal --cycles 5
